# --- SonarQube Cloud (formerly SonarCloud) ---
sonar.organization=pyl0v3r
sonar.projectKey=pyl0v3r_phishing_detector
sonar.projectName=phishing_detector

# Analyze Python as Python 3 (avoid the Py2/3 warning)
sonar.python.version=3.11

# Sources & basic scope
sonar.sources=.
sonar.exclusions=**/venv/**,**/.venv/**,**/__pycache__/**,**/*.md,**/.github/**

# (Optional) If you add tests/coverage later:
# sonar.tests=tests
# sonar.python.coverage.reportPaths=coverage.xml
